# AI Meeting Copilot - Production Environment
# Copy this file to .env.production for production deployment

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=4000
NODE_ENV=production
FRONTEND_URL=https://your-frontend-domain.com

# ============================================
# SECURITY
# ============================================
# CRITICAL: Change these values in production!
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS
DB_PASSWORD=CHANGE_THIS_TO_A_SECURE_DATABASE_PASSWORD

# ============================================
# DATABASE (PostgreSQL for Production)
# ============================================
DATABASE_URL="postgresql://postgres:${DB_PASSWORD}@db:5432/meeting_copilot"

# ============================================
# AI PROVIDER (Choose one)
# ============================================
# Options: gemini, openai
# Recommended: gemini (lower cost, larger context, bigger file limits)
AI_PROVIDER=gemini

# --- Google Gemini (Recommended for Production) ---
# Get your API key from: https://makersuite.google.com/app/apikey
# Advantages:
#   - Lower cost per token
#   - 1M token context window (vs 128K for GPT-4o-mini)
#   - 100MB file size limit (vs 25MB for Whisper)
#   - Single API for transcription + summarization
GEMINI_API_KEY=your-actual-gemini-api-key-here

# --- OpenAI (Alternative) ---
# Get your API key from: https://platform.openai.com
# Uncomment and set AI_PROVIDER=openai to use
# OPENAI_API_KEY=your-actual-openai-api-key-here

# ============================================
# TRANSCRIPTION PROVIDER (Choose one)
# ============================================
# Options: gemini, whisper
# Recommended: gemini (matches AI_PROVIDER for consistency)
TRANSCRIBE_PROVIDER=gemini

# ============================================
# OPTIONAL SETTINGS
# ============================================
# LOG_LEVEL=info
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000
