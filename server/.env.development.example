# ==============================================================
# AI Meeting Copilot — Server
# Copy this file to .env.development for local development
# ==============================================================

# ── Server ───────────────────────────────────────────────────
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ── Security ─────────────────────────────────────────────────
# Both secrets must be at least 32 characters.
# Generate new ones with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_min_32_characters_long_change_this
ENCRYPTION_SECRET=your_encryption_secret_min_32_chars_change_this

# ── Database ─────────────────────────────────────────────────
DATABASE_URL="file:./dev.db"

# ── AI Provider ──────────────────────────────────────────────
# stub   → No API key needed. Best for local development.
# gemini → Users must save their own Gemini key via Settings.
# openai → Users must save their own OpenAI key via Settings.
AI_PROVIDER=stub

# To test live providers locally, temporarily uncomment one key below.
# Never commit real keys. In production, leave these unset — users supply
# their own keys through the Settings page.
# GEMINI_API_KEY=
# OPENAI_API_KEY=

# ── Transcription Provider ───────────────────────────────────
# Must align with AI_PROVIDER:
#   stub   → stub
#   gemini → gemini
#   openai → whisper
TRANSCRIBE_PROVIDER=stub
